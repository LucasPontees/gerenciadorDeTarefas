<div class="tought-form-container">
    <a href="/toughts/dashboard">Voltar</a>
    <h1>editar um Pensamento:</h1>
    <form action="/toughts/edit" method="POST">
        <input type="hidden" name="id" value="{{tought.id}}">
        <div class="form-control">
            <label for="title">Título:</label>
            <input type="text" name="title" placeholder="edite o pensamento" value="{{tought.title}}">
        </div>
        <div class="form-control">
            <label for="description">Descrição:</label>
            <textarea name="description" placeholder="Edite a descrição">{{tought.description}}</textarea>
        </div>
        <input type="submit" value="editar Pensamento">
    </form>
</div>